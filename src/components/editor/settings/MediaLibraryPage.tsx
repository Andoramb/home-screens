'use client';

/** Pictures & videos: the shared media library manager. Stub until the
 *  management page lands; keeps the settings registration testable. */
export default function MediaLibraryPage() {
  return <div data-media-page="" />;
}
